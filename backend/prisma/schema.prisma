// prisma/schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// ... seus modelos Servico e Agendamento

// NOVO: Adicione este bloco
model Servico {
  // ...
}

model Agendamento {
  // ...
}

// Adicione este bloco:
generator client {
  provider = "prisma-client-js"
  // ...
}

datasource db {
  // ...
}

// Configuração do comando de seed
// O Prisma vai procurar este script após o comando db push ou migrate dev
// Mas vamos usá-lo explicitamente no deploy!
// Esta parte é para rodar localmente com o `migrate dev --create-only`
// Não é estritamente necessária para o deploy do Render, mas é boa prática:
// Basta ter o script `prisma:seed` no package.json